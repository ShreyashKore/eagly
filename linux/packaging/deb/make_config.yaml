display_name: Eagly
package_name: eagly
maintainer:
  name: Shreyash Kore
  email: kore.shreyash@gmail.com
priority: optional
section: utils
installed_size: 120000
dependencies:
  - libgtk-3-0
  - liblzma5
  # Runtime for the scrcpy H.264 decoder. The runner links the build machine's
  # libavcodec/libavutil sonames, so these versioned names must match the
  # FFmpeg shipped by the release build image (ubuntu-24.04 → FFmpeg 6.x →
  # libavcodec.so.60 / libavutil.so.58). Keep in sync with release.yml.
  - libavcodec60
  - libavutil58
icon: linux/runner/resources/app_icon.png
generic_name: Device Log Viewer
categories:
  - Utility
  - Development
startup_notify: true

